# Feature slices

Future business modules should be added as vertical feature slices containing their
commands, queries, handlers, and authorization rules. Infrastructure-specific code
must remain outside this directory.
