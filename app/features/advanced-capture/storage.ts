import type{LocalMedia}from"./types";const DB="eventlens-media",STORE="captures";const open=()=>new Promise<IDBDatabase>((resolve,reject)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>r.result.createObjectStore(STORE,{keyPath:"key"});r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)});
export const mediaStorage={put:async(x:LocalMedia)=>{const db=await open();await new Promise<void>((resolve,reject)=>{const t=db.transaction(STORE,"readwrite");t.objectStore(STORE).put(x);t.oncomplete=()=>resolve();t.onerror=()=>reject(t.error)});db.close()},list:async(sessionId:string)=>{const db=await open();const values=await new Promise<LocalMedia[]>((resolve,reject)=>{const r=db.transaction(STORE).objectStore(STORE).getAll();r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)});db.close();return values.filter(x=>x.sessionId===sessionId).sort((a,b)=>b.createdAt.localeCompare(a.createdAt))}};
