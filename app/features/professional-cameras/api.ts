import{apiRequest}from"../../lib/api";import type{CameraConnection,CameraProvider,CapabilityProfile,ProfessionalCamera}from"./types";
const root="/api/booth/professional-cameras";
export const professionalCameraApi={providers:()=>apiRequest<CameraProvider[]>(`${root}/providers`),list:(organizationId:string)=>apiRequest<ProfessionalCamera[]>(`${root}?organizationId=${organizationId}`),register:(body:object)=>apiRequest<ProfessionalCamera>(root,{method:"POST",body:JSON.stringify(body)}),connect:(id:string)=>apiRequest<CameraConnection>(`${root}/${id}/connect`,{method:"POST"}),disconnect:(id:string)=>apiRequest<CameraConnection>(`${root}/${id}/disconnect`,{method:"POST"}),status:(id:string)=>apiRequest<CameraConnection>(`${root}/${id}/status`),capabilities:(id:string)=>apiRequest<CapabilityProfile>(`${root}/${id}/capabilities`)};
