import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("photo template route and canvas composition are wired",async()=>{const app=await readFile("app/App.tsx","utf8"),page=await readFile("app/pages/PhotoTemplates.tsx","utf8"),renderer=await readFile("app/features/photo-templates/canvasRenderer.ts","utf8");assert.ok(app.includes('path="/templates"'));assert.ok(page.includes("localCaptureStorage.list"));assert.ok(page.includes("Download preview"));assert.ok(renderer.includes("layerOrder"));assert.ok(renderer.includes("Math.max(e.width/x.width"));});
test("template client exposes lifecycle operations",async()=>{const api=await readFile("app/features/photo-templates/api.ts","utf8");for(const value of["list:","create:","duplicate:","remove:","active:"])assert.match(api,new RegExp(value));});
